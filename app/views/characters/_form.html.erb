<%= simple_form_for [@source_material, @character] do |f| %>

    <%= f.input :name %>
    <%= f.input :bio %>
    <%= f.input :handle %>

    <%= f.submit class: "small button success radius" %>

<% end %>
