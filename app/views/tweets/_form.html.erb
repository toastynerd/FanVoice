<%= simple_form_for [@source_material, @character, @tweet] do |f| %>

  <%= f.input :body %>
  <%= f.input :post_at, as: :date, discard_year: true, discard_minute: true %>
  <%= f.submit :class => "button" %>
<% end %>