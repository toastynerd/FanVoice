<%= form_for [@source_material, @character, @tweet] do |f| %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.label :post_at %>
  <%= f.text_field :post_at, :class => "datetime" %>
  <%= f.submit :class => "button" %>
<% end %>